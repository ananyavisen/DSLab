{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ in VSCode Terminal",
            "type": "cppvsdbg",   
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "cwd": "${fileDirname}",
            "stopAtEntry": false,
            "preLaunchTask": "build active file",
            "console": "integratedTerminal"
        }
    ]
}
